apiVersion: v1
kind: Service
metadata:
  name: frontend-app-service
  namespace: preprod
spec:
  selector:
   app: frontend-app
  ports:
   - protocol: TCP
     port: 80
     targetport: 80
  type: ClusterIP